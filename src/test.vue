const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'
      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'
      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'
      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'
      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'
      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'
      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'
      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'

      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'
      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'
      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'
      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'
      const { currentUser } = this.$store.getters;
      if (currentUser && this.currentUserIsAdmin) {
        return 'admin';
      }
      return 'user'